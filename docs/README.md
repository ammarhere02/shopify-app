# docs/ — engineering notes

| File | Read it for |
|---|---|
| `PHASES.md` | The build plan in 8 phases, what is done, the automated test counts, and the manual live-store checklists that still need evidence. `[x]` done, `[~]` code and automated tests done but manual checks open, `[ ]` not started |
| `LEARNING.md` | Why each part is built the way it is, phase by phase: the decision, the alternatives, the known limits. This is the source for the architecture note and the reviewer questions in the assignment |

How each folder works today is in that folder's `README.md`, not here.

Still to write for submission (Phase 8): one-page architecture note, ER diagram, OpenAPI or endpoint reference for `/api/v1`, test evidence, time spent per phase. Until then the closest equivalents are `db/README.md` (tables, constraints), `app/routes/README.md` (endpoints, status codes) and `app/services/README.md` (flows).
