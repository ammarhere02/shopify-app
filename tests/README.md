# tests/ — Vitest

## What this folder is
All automated tests. Two kinds: unit tests that need nothing, and integration tests that run the real route handlers and repositories against a real MySQL test database. Shopify itself is always mocked; checks against a live development store are the manual lists in `docs/VERIFICATION.md`.

How the assignment's required tests map to files: badge validation → `enrichment-validation.test.ts`; GraphQL mapping → `product-mapping.test.ts`; a sync decision path → `sync.test.ts`; repository upserts and repeated sync/webhook → `sync.integration.test.ts`, `webhook.integration.test.ts`; request tests (401, tenant scoping, valid write, bad payload, missing product) → `api.integration.test.ts`; raw-body webhook signature tests → `webhook.integration.test.ts`; storefront badge states → `storefront.integration.test.ts` plus the manual theme checklist.

| Command | Runs | Needs |
|---|---|---|
| `npm test` | `*.test.ts` except `*.integration.test.ts` | nothing |
| `npm run test:integration` | only `*.integration.test.ts`, against real MySQL | Docker MySQL + `TEST_DATABASE_URL` (see `.env.example`) |

`scripts/run-integration.mjs` refuses any database that is not local or whose name does not end in `_test`, applies migrations, then runs Vitest with `RUN_MYSQL_TESTS=1`. `vitest.config.ts` switches the include pattern on that variable. Files run one at a time (`fileParallelism: false`).

| File | Covers |
|---|---|
| `graphql-client.test.ts` | Error classification, bounded retries, throttle wait, deadlines, abort |
| `product-mapping.test.ts` | GID/price validation, mapping |
| `sync.test.ts` | Sync decisions with everything mocked (mapping failure, missing cursor, re-auth Response, expired work budget) |
| `enrichment-validation.test.ts` | Badge text, colour, active, all-errors-at-once |
| `sync.integration.test.ts` | Real MySQL, mocked Shopify: pagination, idempotent re-run, enrichment survives, shop isolation, 130 variants, rollback, stale/revive, concurrent starts, abandoned run |
| `enrichment.integration.test.ts` | One enrichment per product, cross-shop access denied, idempotent remove, filters, keyset pagination |
| `webhook.test.ts` | `canReclaim` table, GID from payload, stale-event comparison |
| `webhook.integration.test.ts` | Real MySQL + fake `admin.graphql`: update/create, variant removal, duplicate, stale skip, in-transaction guard, null product, FAILED → retry with same id, tenant isolation, delete idempotency, inactive/unknown shop, concurrent claims, abandoned RECEIVED, log content. Also calls the route `action`s with requests signed by `crypto` HMAC, so the REAL `authenticate.webhook` runs: valid, forged, tampered body (401, no rows), duplicate, `app/uninstalled` receipt |
| `api.test.ts` | Key format/hash, rate limiter with injected clock, product id parsing, cursor round-trip |
| `api.integration.test.ts` | Calls the `/api/v1` route `loader`/`action` with real `Request`s against MySQL: identical 401 for 5 bad-credential kinds, hash-only storage, `lastUsedAt` throttle, failed-login and per-key 429, 405, list filters/pagination, strict 400s, detail, PUT 201/200/422/400/413, cross-shop 404, DELETE 204 twice, sync 202 + background finish, 409 conflict, 409 no session with no run created. `shopify.server` is mocked (only `unauthenticated.admin`) |
| `badge-contrast.test.ts` | Text colour choice; 4.5:1 contrast holds across 4096 sampled colours |
| `storefront.integration.test.ts` | Calls the proxy route with a query signed like Shopify's app proxy (sorted `key=value`, HMAC-SHA256 hex, app secret), so the REAL `authenticate.public.appProxy` runs: active badge (no `internalNote` in body or logs, cache header), 9 identical empty cases incl. cross-shop, tampered/unsigned → 400, 429. Batch route `proxy.badges` with `ids` inside the signed query: several products → map by Shopify id, every unavailable kind absent (checked from both shops), uninstalled/unknown shop → `{}`, 9 malformed/oversized lists → 400 `no-store` and exactly 50 allowed, tampered/unsigned → 400, rate limit shared with the single route and counted per request |
| `generation-state.test.ts` | Job and review transition tables (incl. APPLYING), `canApply`, abandoned-job and abandoned-apply rules, `requireNoUserErrors`, input hash (stable across key/media order, changes with every input part), AI config parsing (defaults, ranges, missing key/models, error never echoes the key), model allowlist |
| `generation.integration.test.ts` | Real MySQL: idempotent `createJob` (repeat, 5 concurrent, key scoped per shop), foreign/deleted product and foreign `previousJobId` refused, regeneration link, cross-shop reads and writes denied, start-once, complete = output + first draft atomically, no output row for failed jobs, bounded error, abandoned jobs (only old, only that shop), daily count, draft edit leaves `rawJson`/`validatedJson` unchanged, review walk, one winner of two concurrent applies, rollback with the surrounding transaction, version history + restore row, publication action audit |
| `html-sanitize.test.ts` | Allowed tags kept, all attributes removed, script/style/iframe/svg removed with content, links and images removed, malformed and nested markup, 500-round property check (output holds only allowed attribute-less tags, no stray `<`, sanitizing twice is a no-op), balancing, entities |
| `description-output.test.ts` | Output validation (not JSON, truncated, extra/missing field, wrong type, over-long description and runaway fields fail, over-long suggestion fields are shortened at a word boundary with a warning and the raw answer kept, fence unwrapping, sanitized HTML with raw kept, empty after sanitizing, schema = validator), claim detection per category and against trusted text, prompt builder (system vs data, text before images, a hostile title/context/alt cannot close a data block, bounded context) |
| `openrouter-client.test.ts` | Injected `fetch`, no network: request body (strict `json_schema`, `require_parameters`, data policy, part order), usage and missing usage, retry on 429/5xx/timeout/network and on an error inside a 200 body, `Retry-After` honoured and capped, retry exhaustion, no retry on 401/402/400/404, refusal, empty and truncated answers, logs never contain the key, prompt, image URL or answer |
| `description-generation.integration.test.ts` | Real MySQL, fake Shopify client, fake model client: QUEUED then run → sanitized draft, raw kept, warnings (model + detected, supported claim not flagged), usage; snapshot with description hash; only the product's Shopify images sent; idempotent repeat (no second Shopify or model call), 4 simultaneous identical requests → 1 job, a retry is never rate-limited; invalid JSON / extra field / REFUSED / RATE_LIMITED / TIMEOUT → FAILED with a diagnosable error and no retry; internal error text hidden; 9 invalid requests incl. URL and metadata-address instead of a media id (nothing stored, Shopify not called); foreign and not-ready media; cross-shop product, key and previous job; product gone from Shopify; concurrency limit per shop, two simultaneous different requests → one passes, daily limit counts failures, abandoned job frees the slot and its late run does nothing; logs hold identifiers only |
| `generation-api.integration.test.ts` | Real route handlers, services and MySQL; `shopify.server` (offline + admin session) and `createOpenRouterClient` are the only fakes. API: 202 + `Location` then polling to a sanitized draft with usage, nothing hidden leaks into the response, same `Idempotency-Key` → 200 and one model call, 401 envelope, other shop's product/job → 404, seven 422 cases (URL instead of media id, foreign image, oversized context …) with nothing stored, missing key 422 / non-object 400 / huge body 413, 429 while a generation runs, 503 not configured, 409 no session, failed job readable through GET, lists, images, regenerate (linked, reuses or overrides input, idempotent, 409 while running). Admin route: generate → poll → edit (sanitized, model text untouched) → approve → reopen → reject with one Shopify call in total, field errors, empty/oversized draft, wrong product or wrong shop → not found, not-configured message |
| `description-apply.integration.test.ts` | Real routes, services and MySQL; Shopify faked as a tiny in-memory product store (read, `productUpdate`, publications, `publishablePublish`) so writes can be asserted. Apply: version with before/after + Shopify `updatedAt`, APPLIED, local `updatedAtShopify` refreshed, one read + one mutation; apply once (409), two simultaneous → one write; stale 409 with no mutation and the job back to APPROVED; own text not stale; `userErrors` 422 retryable; transport 500 retryable; APPLYING recovered after the window; missing scope 403 with no Shopify call; other shop 404, no key 401. Restore: older version and "previous" text, linked rows, order of history, bad `which` 422, foreign version 404. Publish: channel list with state, success audit, DRAFT product 409, unknown/malformed channel 422 without audit row, `userErrors` → FAILED row + 422, missing scope 403, other shop 404. Admin route: apply/publish/restore with `admin:<shop>` actor, `?publications=1`, STALE code. Hardening (real redaction captured): apply/publish log lines hold identifiers only (no HTML, key or header); Shopify throttle → 429 `shopify_throttled` + `Retry-After`, transport → 502, job stays APPROVED and retries succeed; admin page gets `SHOPIFY_<kind>` + plain sentence without Shopify's text; admin write intents limited 10/min per shop, reads unaffected, other shop unaffected |
| `hardening.test.ts` | Logger redaction (key names at every depth, credential-shaped values under innocent keys, `data:` URIs, 500-char cut, depth bound, ordinary values untouched); `ShopifyApiError` → API envelope per kind without Shopify's text, `Retry-After` on throttle; admin wording |
| `fixtures.ts` | `product(id, variants)` builds a GraphQL product node |

## Rules
- Integration tests create their own shops (random domain) and delete only those in `afterAll`. Never truncate or reset a database.
- Shopify is always mocked here. Live checks are the manual checklist in `docs/VERIFICATION.md`.
- `webhook.integration.test.ts` and `storefront.integration.test.ts` set `SHOPIFY_API_SECRET` etc. inside `vi.hoisted`, because `shopify.server.ts` reads env at import time and ES imports run before normal statements.
- The webhook test deletes only the receipts whose ids it generated.
- API tests call `resetRateLimitsForTests()` in `beforeEach`, because the limiters are module-level state.
- Liquid and the block's JS cannot run in Vitest. `shopify theme check --path extensions/product-badge` lints them; rendering is on the manual checklist in `docs/VERIFICATION.md`.
- Not covered yet: `products/update` through the route with a real session is not automated (needs a live token); it is on the manual checklist.
